console.log('Lets learn about variables'); //this must be run through the terminal

/**
 * JavaScript Variables
 */

 var value = 100;
 value = 200 //this reassigned will display instead of 100
 value = "King Rudy"; //this will now display over the other values since it has been reassigned
 value = true;
 console.log(value);
 var num1 = 100, num2 = 300; //can declare multiple variables within the same line
 var result = num1 + num2;
 console.log(result);

 /** How to know the type of a variable or object in JavaScript? */

 console.log(typeof(value)); //will show the variable type stored in value

 //Switch between code window to terminal: ctrl + ~

